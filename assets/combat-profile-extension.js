(function(global){
'use strict';
var Model=global.SemideusesCharacter,db=global.SemideusesRulesDatabase;if(!Model||!db)return;
var oldNormalize=Model.normalize,oldCalculate=Model.calculate,oldCreate=Model.create;
var DAMAGE=['Cortante','Perfurante','Concussivo','Ígneo','Elétrico','Gélido','Ácido','Psíquico','Divino','Necrótico','Radiante','Místico','Veneno','Sônico'];
var CONDITIONS=['Abalado','Agarrado','Apavorado','Atordoado','Caído','Cego','Dominado','Enfeitiçado','Envenenado','Incapacitado','Invisível','Morrendo','Paralisado','Petrificado','Restrito','Surdo'];
var EXTRA_CONDITIONS=['Dominado','Invisível','Petrificado','Surdo'];
function clone(v){return Model.clone(v);}function norm(v){return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();}
function add(list,value,source,kind){if(!value)return;var key=norm(value);if(!list.some(function(x){return norm(x.value)===key&&norm(x.source)===norm(source||'');}))list.push({value:value,source:source||'',kind:kind||''});}
function parseText(profile,text,source){text=String(text||'');if(!text)return;var s=norm(text);
  DAMAGE.forEach(function(t){var n=norm(t);if(s.indexOf('resistencia a dano '+n)>=0||s.indexOf('resistencia a '+n)>=0||s.indexOf('resistente a '+n)>=0||s.indexOf('resistencia ('+n+')')>=0)add(profile.resistances,t,source,'damage');if(s.indexOf('imune a dano '+n)>=0||s.indexOf('imunidade a dano '+n)>=0)add(profile.immunities,t,source,'damage');});
  CONDITIONS.forEach(function(t){var n=norm(t);if(s.indexOf('imunidade a condicao '+n)>=0||s.indexOf('imune a condicao '+n)>=0||s.indexOf('imune a '+n)>=0)add(profile.conditionImmunities,t,source,'condition');});
  if(s.indexOf('imune as condicoes')>=0||s.indexOf('imunidade as condicoes')>=0){CONDITIONS.forEach(function(t){if(s.indexOf(norm(t))>=0)add(profile.conditionImmunities,t,source,'condition');});}
  if(/Vantagem em Testes de Resistência|Vantagem contra|dano .* reduzido|não pode receber a condição|efeitos mágicos têm duração|venenos e doenças|penalidades de combate subaquático|reduz todo dano sofrido/i.test(text))add(profile.defensiveNotes,text,source,'note');
}
function equipped(c){var out=[],slots=c.equipmentSlots||{};Object.keys(slots).forEach(function(k){var id=slots[k],r=(c.inventory||[]).find(function(x){return x.id===id;});if(!r)return;var d=Model.equipmentDefinition?Model.equipmentDefinition(r):r;if(d)out.push({record:r,definition:d,slot:k});});return out;}
function directPanoply(c){var mythic=c.mythic||{},id=String(mythic.panoplyId||'');if(!id)return null;if(mythic.customPanoply&&mythic.customPanoply.id===id)return clone(mythic.customPanoply);var d=db.getMythicItem&&db.getMythicItem(id);return d&&d.tier==='Panóplia'?d:null;}
function profile(c){var p={resistances:[],immunities:[],conditionImmunities:[],defensiveNotes:[],equipmentEffects:[]},aff=db.getAffiliation&&db.getAffiliation(c.affiliation);
  if(aff){(aff.abilities||[]).filter(function(a){return Number(a.level||1)<=Number(c.level||1)&&(/passiva/i.test(String(a.action||''))||/passiva/i.test(String(a.rank||''))||a.name==='Dom');}).forEach(function(a){parseText(p,a.effect,c.affiliation+' — '+a.name);});var path=(aff.paths||[]).find(function(x){return x.name===c.divinePath;});if(path)(path.abilities||[]).filter(function(a){return Number(a.level||1)<=Number(c.level||1)&&(/passiva/i.test(String(a.action||''))||/passiva/i.test(String(a.rank||'')));}).forEach(function(a){parseText(p,a.effect,path.name+' — '+a.name);});}
  (c.talents||[]).forEach(function(r){var t=db.getTalent&&db.getTalent(r.catalogId||r.name);if(t)parseText(p,t.effect,'Talento — '+t.name);});
  equipped(c).forEach(function(x){var d=x.definition,src='Equipamento — '+d.name,text=[d.notes||'',(d.properties||[]).join(' · ')].filter(Boolean).join(' · ');parseText(p,text,src);if(text)add(p.equipmentEffects,text,src,'equipment');if(x.record.forge&&c.rules&&c.rules.equipment&&c.rules.equipment.forgeNotes){var f=c.rules.equipment.forgeNotes.find(function(n){return n.itemId===x.record.id;});if(f&&(f.notes||[]).length){var ft=f.notes.join(' · ');parseText(p,ft,'Forja — '+d.name);add(p.equipmentEffects,ft,'Forja — '+d.name,'forge');}}});
  var pan=directPanoply(c);if(pan){parseText(p,pan.base,'Panóplia — '+pan.name+' (Base)');if(pan.base)add(p.equipmentEffects,pan.base,'Panóplia — '+pan.name,'panoply');[5,11,17].forEach(function(level){if(Number(c.level||1)>=level&&pan.awakenings&&pan.awakenings[String(level)]){var e=pan.awakenings[String(level)];parseText(p,e,'Panóplia — '+pan.name+' (Desperta '+level+')');add(p.equipmentEffects,e,'Panóplia — '+pan.name+' (Desperta '+level+')','panoply');}});}
  return p;
}
function fixOffhand(c){var attacks=c.rules&&c.rules.equipment&&c.rules.equipment.attacks||[];attacks.forEach(function(a){if(a.slot!=='offHand')return;var mod=global.SemideusesRules&&global.SemideusesRules.modifier?global.SemideusesRules.modifier(c.attributes[a.attribute]):0;if(a.damage&&mod>0)a.damage=a.damage.replace(new RegExp('\\s[+]\\s'+mod+'(?=\\s|$)'),'');else if(a.damage&&mod<0)a.damage=a.damage.replace(new RegExp('\\s-\\s'+Math.abs(mod)+'(?=\\s|$)'),'');a.damage=String(a.damage||'').replace(/\s{2,}/g,' ').trim();a.offHandNoAttribute=true;a.actionType='Ação Bônus';a.attacksPerAction=1;});return c;}
function restoreExtra(c,raw){var src=raw&&raw.resources||{},incoming=[];if(Array.isArray(src.conditions))incoming=incoming.concat(src.conditions);if(src.condition)incoming.push(src.condition);c.resources=c.resources||{};c.resources.conditions=Array.isArray(c.resources.conditions)?c.resources.conditions:[];incoming.forEach(function(v){if(EXTRA_CONDITIONS.indexOf(v)>=0&&c.resources.conditions.indexOf(v)<0)c.resources.conditions.push(v);});if(c.resources.conditions.length)c.resources.condition=c.resources.conditions[0];return c;}
function apply(c,raw){restoreExtra(c,raw);fixOffhand(c);c.rules=c.rules||{};c.rules.combatProfile=profile(c);return c;}
function normalize(v){var raw=clone(v||{});return apply(oldNormalize(raw),raw);}function calculate(v){var raw=clone(v||{});return apply(oldCalculate(raw),raw);}function create(v){var raw=clone(v||{});return apply(oldCreate(raw),raw);}
Model.normalize=normalize;Model.calculate=calculate;Model.create=create;Model.combatProfile=function(c){var n=Model.normalize(c);return clone(n.rules.combatProfile||{});};Model.CONDITIONS=(Model.CONDITIONS||[]).concat(EXTRA_CONDITIONS.filter(function(x){return (Model.CONDITIONS||[]).indexOf(x)<0;}));Model.version='3e-model-0.12.2';
})(window);
