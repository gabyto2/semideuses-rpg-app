(function(global){
  'use strict';
  var Service=global.SemideusesCharacterService,Model=global.SemideusesCharacter,db=global.SemideusesRulesDatabase,Runtime=global.SemideusesMythicRuntime,App=global.SemideusesApp;if(!Service||!Model||!db||!Runtime)return;
  var tab='panoply',catalogTier='Consumível',query='',rendering=false,scheduled=false;
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  function character(){var e=App&&App.getEditing&&App.getEditing();return e&&e.id?Service.get(e.id)||e:null;}
  function restoreRefresh(){var x=global.scrollX,y=global.scrollY;if(App&&App.refresh)App.refresh();setTimeout(function(){global.scrollTo(x,y);},0);schedule();}
  function actionLabel(a){return a||'Ativação não especificada';}
  function panoplyTab(c){var p=Model.panoply(c);if(!p)return '<div class="mythic-empty"><span>✦</span><h3>Nenhuma Panóplia vinculada</h3><p>Cada herói pode portar exatamente uma Panóplia ligada à alma. Ela desperta nos níveis 5, 11 e 17.</p><button class="primary" data-open-mythic-catalog="Panóplia">Escolher Panóplia</button></div>';
    var wakes=Model.panoplyAwakenings(c),check=Runtime.canUsePanoply(c),a=p.active||{};
    return '<div class="mythic-panoply-head"><div><span class="eyebrow">PANÓPLIA LIGADA À ALMA</span><h3>'+esc(p.name)+'</h3><small>'+esc(p.sourceGroup||'')+'</small></div><button class="danger-link" data-unlink-panoply>Corrigir vínculo</button></div>'+(p.source?'<p class="mythic-lore">'+esc(p.source)+'</p>':'')+'<article class="panoply-stage unlocked"><span>BASE</span><p>'+esc(p.base||'')+'</p></article><div class="panoply-awaken-grid">'+wakes.map(function(w){return '<article class="panoply-stage '+(w.unlocked?'unlocked':'locked')+'"><span>NÍVEL '+w.level+(w.unlocked?' · DESPERTO':' · BLOQUEADO')+'</span><p>'+esc(w.effect)+'</p></article>';}).join('')+'</div><article class="panoply-active"><div><span class="eyebrow">ATIVA</span><h4>'+esc(a.name||'Ativa da Panóplia')+'</h4><small>Rank '+esc(a.rank||'—')+' · '+Number(a.cost||0)+' '+esc(c.rules.primaryResource.label)+' · '+esc(actionLabel(a.action))+' · '+esc(a.recovery==='shortRest'?'Descanso Curto':a.recovery==='longRest'?'Descanso Longo':'Recarga não indicada')+'</small></div><p>'+esc(a.effect||'')+'</p><button class="primary" data-use-panoply '+(check.allowed?'':'disabled title="'+esc(check.reason||'Indisponível')+'"')+'>Ativar '+esc(a.name||'Panóplia')+'</button>'+(a.action?'':'<small class="sheet-note">O texto da Panóplia não especifica Ação/Ação Bônus/Reação; o aplicativo não inventa esse custo.</small>')+'</article>';
  }
  function consumableCard(c,r){var d=db.getMythicItem(r.catalogId),variants=d&&d.variants||[],check=variants.length?null:Runtime.canUseConsumable(c,r.id);return '<article class="mythic-consumable"><details><summary><span><strong>'+esc(d.name)+'</strong><small>'+esc(d.sourceGroup||'Consumível')+' · '+(r.charges)+' '+esc(d.chargeLabel||'uso(s)')+'</small></span><b>'+r.charges+'</b></summary><div>'+(d.source?'<p class="mythic-lore">'+esc(d.source)+'</p>':'')+'<p>'+esc(d.effect||'')+'</p>'+(d.action?'<div class="equipment-tags"><span>'+esc(d.action)+'</span></div>':'')+(variants.length?'<div class="mythic-variant-actions">'+variants.map(function(v){var count=Number(r.variantCharges&&r.variantCharges[v.id]||0),vcheck=Runtime.canUseConsumable(c,r.id,v.id);return '<button class="secondary" data-use-consumable="'+r.id+'" data-variant="'+v.id+'" '+(vcheck.allowed?'':'disabled title="'+esc(vcheck.reason||'Indisponível')+'"')+'>'+esc(v.label)+' · '+count+'</button>';}).join('')+'</div>':'<button class="primary" data-use-consumable="'+r.id+'" '+(check&&check.allowed?'':'disabled title="'+esc(check&&check.reason||'Indisponível')+'"')+'>Usar · restam '+r.charges+'</button>')+'<button class="danger-link" data-remove-mythic-consumable="'+r.id+'">Remover do inventário</button></div></details></article>';}
  function consumablesTab(c){var list=c.mythic&&c.mythic.consumables||[];return '<div class="mythic-tab-head"><div><span class="eyebrow">CONSUMÍVEIS MÍTICOS</span><h3>Doses e usos</h3><p>Usar desconta a dose e respeita a economia de ações quando o livro informa a ativação.</p></div><button class="secondary" data-open-mythic-catalog="Consumível">+ Adicionar</button></div>'+(list.length?'<div class="mythic-consumable-list">'+list.map(function(r){return consumableCard(c,r);}).join('')+'</div>':'<div class="command-empty"><p>Nenhum consumível mítico registrado.</p></div>');}
  function catalogCard(c,i){var owned=i.tier==='Panóplia'&&c.mythic.panoplyId===i.id;var text=i.tier==='Consumível'?i.effect:(i.base+' '+Object.keys(i.awakenings||{}).map(function(k){return 'Desperta '+k+': '+i.awakenings[k];}).join(' ')+' Ativa — '+(i.active&&i.active.name||'')+': '+(i.active&&i.active.effect||''));return '<details class="mythic-catalog-card"><summary><span><strong>'+esc(i.name)+'</strong><small>'+esc(i.sourceGroup||i.tier)+'</small></span><b>'+esc(i.tier)+'</b></summary><div>'+(i.source?'<p class="mythic-lore">'+esc(i.source)+'</p>':'')+'<p>'+esc(text)+'</p>'+(i.tier==='Consumível'?'<div class="equipment-tags"><span>'+esc(i.defaultCharges?i.defaultCharges+' '+i.chargeLabel:i.chargeFormula||i.chargeLabel)+'</span>'+(i.action?'<span>'+esc(i.action)+'</span>':'')+'</div><button class="secondary" data-add-mythic-consumable="'+i.id+'">Adicionar ao inventário</button>':'<button class="'+(owned?'secondary':'primary')+'" data-link-panoply="'+i.id+'" '+(owned?'disabled':'')+'>'+(owned?'Vinculada':'Vincular Panóplia')+'</button>')+'</div></details>';}
  function catalogTab(c){var all=db.listMythicItems?db.listMythicItems(catalogTier):[],q=query.trim().toLowerCase();if(q)all=all.filter(function(i){return (i.name+' '+i.sourceGroup+' '+(i.effect||i.base||'')).toLowerCase().indexOf(q)>=0;});return '<div class="mythic-catalog-controls"><div class="equipment-tabs"><button class="'+(catalogTier==='Consumível'?'active':'')+'" data-mythic-tier="Consumível">43 Consumíveis</button><button class="'+(catalogTier==='Panóplia'?'active':'')+'" data-mythic-tier="Panóplia">52 Panóplias</button></div><label>Buscar<input data-mythic-search value="'+esc(query)+'" placeholder="Nome, mito ou efeito"></label><span>'+all.length+' resultado(s)</span></div><div class="mythic-catalog-list">'+all.map(function(i){return catalogCard(c,i);}).join('')+'</div>';}
  function body(c){return tab==='consumables'?consumablesTab(c):tab==='catalog'?catalogTab(c):panoplyTab(c);}
  function html(c){return '<section class="panel mythic-center" data-mythic-center><div class="section-heading"><div><span class="eyebrow">ITENS MÍTICOS</span><h2>Panóplia & Consumíveis</h2><p>Itens conquistados pela história, separados do equipamento mundano.</p></div></div><nav class="mythic-tabs"><button class="'+(tab==='panoply'?'active':'')+'" data-mythic-tab="panoply">Panóplia</button><button class="'+(tab==='consumables'?'active':'')+'" data-mythic-tab="consumables">Consumíveis <b>'+((c.mythic&&c.mythic.consumables||[]).length)+'</b></button><button class="'+(tab==='catalog'?'active':'')+'" data-mythic-tab="catalog">Catálogo</button></nav><div class="mythic-body">'+body(c)+'</div></section>';}
  function render(){if(rendering)return;var anchor=document.querySelector('[data-equipment-center]'),c=character();if(!anchor||!c)return;rendering=true;try{var old=document.querySelector('[data-mythic-center]'),markup=html(c);if(old)old.outerHTML=markup;else anchor.insertAdjacentHTML('afterend',markup);}finally{rendering=false;}}
  function schedule(){if(scheduled)return;scheduled=true;setTimeout(function(){scheduled=false;render();},0);}
  document.addEventListener('click',function(e){var c=character();if(!c)return;var t=e.target.closest('[data-mythic-tab]');if(t){tab=t.dataset.mythicTab;render();return;}var open=e.target.closest('[data-open-mythic-catalog]');if(open){catalogTier=open.dataset.openMythicCatalog;tab='catalog';render();return;}var tier=e.target.closest('[data-mythic-tier]');if(tier){catalogTier=tier.dataset.mythicTier;render();return;}var add=e.target.closest('[data-add-mythic-consumable]');if(add){try{var d=db.getMythicItem(add.dataset.addMythicConsumable),amount=null;if(d&&d.chargeFormula){amount=prompt('Quantidade obtida ('+d.chargeFormula+' '+d.chargeLabel+'):','');if(amount===null)return;}Service.addMythicConsumable(c.id,add.dataset.addMythicConsumable,amount);tab='consumables';restoreRefresh();}catch(err){alert(err.message);}return;}var use=e.target.closest('[data-use-consumable]');if(use){try{Runtime.useConsumable(c.id,use.dataset.useConsumable,use.dataset.variant||'');restoreRefresh();}catch(err){alert(err.message);}return;}var rem=e.target.closest('[data-remove-mythic-consumable]');if(rem){if(confirm('Remover este consumível da ficha?'))try{Service.removeMythicConsumable(c.id,rem.dataset.removeMythicConsumable);restoreRefresh();}catch(err){alert(err.message);}return;}var link=e.target.closest('[data-link-panoply]');if(link){try{var replace=!!c.mythic.panoplyId;if(replace&&!confirm('O personagem já possui uma Panóplia. Trocar o vínculo apenas como correção/autorização do Mestre?'))return;Service.linkPanoply(c.id,link.dataset.linkPanoply,replace);tab='panoply';restoreRefresh();}catch(err){alert(err.message);}return;}if(e.target.closest('[data-unlink-panoply]')){if(confirm('Remover o vínculo apenas para corrigir a ficha? A Panóplia normalmente é ligada à alma.'))try{Service.unlinkPanoply(c.id);restoreRefresh();}catch(err){alert(err.message);}return;}if(e.target.closest('[data-use-panoply]')){try{Runtime.usePanoply(c.id);restoreRefresh();}catch(err){alert(err.message);}return;}});
  document.addEventListener('input',function(e){if(e.target.matches('[data-mythic-search]')){query=e.target.value;render();}});
  global.addEventListener('semideuses:character-updated',schedule);global.addEventListener('load',schedule);new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});schedule();
})(window);
